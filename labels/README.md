# `protection.jsonl` — kept, no longer written

**Nothing in Devoid appends to this file as of 2026-09-07 10:54 EDT.** The writer was removed on Harkirat's call: *"drop the labels from the app. it's just adding friction and the repo has its own corpus that i supply it anyway."*

**The file and its history stay.** An append-only log is not deleted; it stops being appended to. It currently holds **0 rows**, which was always the honest answer — no ambiguous asset had been answered outside a test.

⚠️ **The engine repo points at this path by name** — `scripts/harness/labels/README.md` in `/Applications/Claude Code/Gif-Background-Remover` says Devoid *"records every answer as a labelled row"* here. **That sentence is now false and the pointer needs correcting in that repo.** It is a cross-repo change and is flagged rather than made silently.

**Where labelled data actually comes from:** the engine repo's own harness, supplied deliberately. It holds 981 labels for `edge_hardness`. The protection decision has none, and collecting them as a byproduct of answering was the idea this removal retires.
